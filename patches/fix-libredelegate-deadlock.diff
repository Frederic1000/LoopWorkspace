diff --git a/LoopKit/LoopKit/WeakSynchronizedDelegate.swift b/LoopKit/LoopKit/WeakSynchronizedDelegate.swift
index a672649..abd5147 100644
--- a/LoopKit/LoopKit/WeakSynchronizedDelegate.swift
+++ b/LoopKit/LoopKit/WeakSynchronizedDelegate.swift
@@ -72,12 +72,18 @@ public class WeakSynchronizedDelegate<Delegate> {
     }
 
     public func call<ReturnType>(_ block: (_ delegate: Delegate?) -> ReturnType) -> ReturnType {
-        return lock.withLock { () -> ReturnType in
-            var result: ReturnType!
-            _queue.sync {
-                result = block(_delegate as? Delegate)
-            }
-            return result
+        var delegate: Delegate?
+        var queue: DispatchQueue!
+
+        lock.withLock {
+            delegate = _delegate as? Delegate
+            queue = _queue
+        }
+
+        var result: ReturnType!
+        queue.sync {
+            result = block(delegate)
         }
+        return result
     }
 }
